/aha
*.test
*.out
.DS_Store
coverage.out

# Installed agent skills (dev tooling, not project source)
/.agents/
/skills-lock.json

# Harness-managed agent worktrees (transient scratch, not project source)
/.claude/
/.pi-subagents/
internal/corpus/testdata/rapid/

# Wrangler's machine-local account cache (never project source)
/.wrangler/
